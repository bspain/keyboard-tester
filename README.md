# Keyboard Tester

Minimal Electron keyboard tester with a keypress logger and realtime key highlighting.

## Scripts
- `npm install`
- `npm run start`

## Windows packaging
- `npm install`
- `npm run dist`
